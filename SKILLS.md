# 🗺️ Карта навыков — parser_agent

> Последнее обновление: 13.05.2026  
> Строк кода: 3 254 | Модулей: 17 | Команд бота: 20 | Зависимостей: 13

---

## ✅ Реализовано

### 🥷 Парсинг и обход защит

| Навык | Файл | Описание |
|---|---|---|
| **Ninja Ozon Parser** | `app/updater.py` | Playwright, случайный UA из 7 вариантов, скролл страницы, движение мыши, stealth JS (скрытие webdriver, plugins, languages), задержки как у человека |
| **Wildberries API Parser** | `app/parsers/wildberries.py` | Без браузера — через API. 3 версии эндпоинта с автофоллбэком, поиск по ID. Получает: цену, старую цену, скидку, рейтинг, кол-во отзывов, бренд, изображение |
| **Marketplace Router** | `app/parsers/router.py` | Автоопределяет Ozon / Wildberries / AliExpress по URL. Выбирает нужный парсер автоматически |
| **Unified ProductData** | `app/parsers/base.py` | Единая структура данных для всех маркетплейсов: name, price, old_price, discount_pct, availability, rating, reviews, seller, brand, marketplace |
| **Retry механизм** | `app/parsers/wildberries.py` | 3 попытки с разными API эндпоинтами, фоллбэк на поисковый API |
| **Определение блокировки** | `app/updater.py` | Детектирует страницы-заглушки Озона, ставит статус `blocked` вместо `deleted` |

---

### 🗄️ База данных

| Навык | Файл | Описание |
|---|---|---|
| **SQLAlchemy Async ORM** | `app/database.py` | Полностью асинхронная БД. SQLite по умолчанию, PostgreSQL — одна строка в `.env` |
| **История цен** | `app/database.py` | Таблица `price_history`. Новая запись только при изменении цены или статуса — дубли не пишутся |
| **Дедупликация товаров** | `app/database.py` | MD5-хеш URL как уникальный ключ. Повторный `/add` обновляет товар, не дублирует |
| **Статусы товаров** | `app/database.py` | `in_stock` / `out_of_stock` / `blocked` (временная блокировка) / `deleted` (404) |

---

### 🤖 AI и аналитика

| Навык | Файл | Описание |
|---|---|---|
| **Grok API (xAI)** | `app/agent.py`, `app/ai_analyzer.py` | Основной AI провайдер. Модель `grok-3-mini`. Настраивается через `GROK_API_KEY` в `.env` |
| **Claude API (запасной)** | `app/agent.py` | Автофоллбэк на Claude если нет GROK_API_KEY. `claude-opus-4-5` |
| **Прогноз цен** | `app/ai_analyzer.py` | Линейная регрессия на чистом Python. Прогноз на 7 и 30 дней вперёд |
| **Паттерны цен** | `app/ai_analyzer.py` | `rising` / `falling` / `stable` / `volatile` — определяется по общему тренду |
| **Обнаружение аномалий** | `app/ai_analyzer.py`, `app/agent.py` | Скачок цены >20% за один шаг → алерт. Новый минимум за 30 дней → алерт |
| **Лучшие сделки** | `app/agent.py`, `app/ai_analyzer.py` | Товары на минимальной цене за 30 дней, сортировка по % выгоды |
| **Обзор рынка** | `app/ai_analyzer.py` | Сводный отчёт: топ сделок, растущие, падающие, AI комментарий |
| **Анализ отзывов** | `app/agent.py` | Через LLM: позитив / негатив / нейтраль, признаки фейков, плюсы и минусы товара |
| **Сравнение маркетплейсов** | `app/agent.py` | Сравнение цен Озон vs WB на один товар с AI советом |

---

### 📊 Экспорт и отчёты

| Навык | Файл | Описание |
|---|---|---|
| **HTML отчёт** | `app/reporter.py` | Интерактивный отчёт с Chart.js: карточки товаров со спарклайнами, тепловая карта активности, диаграмма распределения цен, рекомендации. Открывается в браузере |
| **Excel экспорт** | `app/exporter.py` | openpyxl: цветовое форматирование по маркетплейсу и наличию, заморозка строк, мин/макс цены за 30 дней |
| **CSV экспорт** | `app/exporter.py` | utf-8-sig кодировка для корректного отображения кириллицы в Excel |

---

### 🤖 Telegram бот

| Навык | Файл | Описание |
|---|---|---|
| **20 команд бота** | `app/bot.py` | Полный список ниже |
| **FSM диалоги** | `app/bot.py` | aiogram 3 FSM: ожидание URL от пользователя, поискового запроса, выбора товара из результатов |
| **Фоновый запуск** | `app/worker.py` | Парсер запускается в фоне, прогресс отправляется в чат по мере работы |
| **Подписки** | `app/bot.py` | `/subscribe` — рассылка об изменении цен после каждого `/update` |
| **SSL фикс Windows** | `app/bot.py` | `certifi` + `AiohttpSession._connector_init` — решает `APPLICATION_DATA_AFTER_CLOSE_NOTIFY` |
| **Защита доступа** | `app/bot.py` | `ADMIN_IDS` в `.env` — только указанные пользователи могут запускать парсер |

#### Полный список команд бота

| Команда | Описание |
|---|---|
| `/start`, `/help` | Список всех команд |
| `/add` | Добавить товар(ы) по ссылке — Озон или WB автоматически |
| `/wb` | Добавить товар с Wildberries |
| `/update` | Обновить цены всех товаров |
| `/list` | Список отслеживаемых товаров с текущими ценами |
| `/search` | Найти товары по названию на Озоне |
| `/compare` | Сравнить цены между маркетплейсами |
| `/analyze` | AI обзор всего портфеля |
| `/deep_analyze` | Глубокий AI анализ рынка |
| `/deals` | Лучшие сделки (минимум цены за 30 дней) |
| `/anomalies` | Подозрительные изменения цен + AI комментарий |
| `/forecast` | Прогноз цен на 7 дней |
| `/alerts` | Активные алерты (новые минимумы, скачки) |
| `/reviews` | AI анализ отзывов товара |
| `/export_csv` | Экспорт в CSV |
| `/export_excel` | Экспорт в Excel |
| `/report` | HTML отчёт с графиками |
| `/status` | Статистика базы данных |
| `/subscribe` | Подписаться на уведомления |
| `/unsubscribe` | Отписаться |

---

### 🔧 Инфраструктура

| Навык | Файл | Описание |
|---|---|---|
| **.env конфигурация** | `.env`, `.env.example`, `app/config.py` | Все настройки в `.env`, токены вне кода, шаблон `.env.example` |
| **Поддержка прокси** | `app/updater.py`, `app/worker.py` | `PROXY=http://user:pass@ip:port` в `.env`, передаётся в Playwright |
| **Логирование** | `app/config.py` | RotatingFileHandler — `parser.log` с ротацией 10МБ |
| **README** | `README.md` | Полная документация с командами запуска |

---

## ⚠️ Частично реализовано

| Навык | Проблема | Что сделать |
|---|---|---|
| **Статус `deleted`** | Ставится при любой ошибке загрузки, не только при 404 | Исправить в `updater.py`: `deleted` только при HTTP 404, `blocked` при остальных ошибках |

---

## 📋 Запланировано (не реализовано)

| Навык | Приоритет | Описание |
|---|---|---|
| **APScheduler** | 🔴 высокий | Автообновление цен каждые N часов без команды `/update`. Добавить в `bot.py` |
| **AliExpress Parser** | 🟡 средний | Заготовка в `parsers/base.py` есть, нужна реализация через Playwright |
| **Деплой на VPS** | 🟡 средний | Ubuntu сервер `94.103.15.69` готов, нужны скрипты установки |
| **pytest тесты** | 🟢 низкий | Только ручные smoke-тесты, нужны автоматические unit-тесты |
| **График цен картинкой** | 🟢 низкий | Отправлять PNG график прямо в Telegram (matplotlib или plotly) |

---

## 📦 Зависимости

```
aiogram==3.4.1          # Telegram бот
sqlalchemy==2.0.28      # ORM база данных
aiosqlite==0.19.0       # Async SQLite драйвер
aiohttp==3.9.3          # HTTP клиент
beautifulsoup4==4.12.3  # Парсинг HTML
lxml==5.1.0             # HTML парсер (быстрый)
python-dotenv==1.0.1    # Загрузка .env
tenacity==8.2.3         # Retry механизм
certifi>=2024.2.2       # SSL сертификаты (фикс Windows)
playwright==1.42.0      # Браузерная автоматизация
anthropic>=0.25.0       # Claude API (запасной AI)
openpyxl>=3.1.0         # Excel экспорт
pandas>=2.0.0           # Обработка данных
```

---

## 🏗️ Структура проекта

```
parser_agent/
├── .env                    ← токены и настройки (не в git!)
├── .env.example            ← шаблон настроек
├── requirements.txt        ← зависимости
├── run.sh                  ← быстрый запуск
├── README.md               ← документация
├── SKILLS.md               ← этот файл
└── app/
    ├── main.py             ← точка входа (--telegram / --update)
    ├── bot.py              ← Telegram бот, 20 команд
    ├── config.py           ← загрузка .env
    ├── database.py         ← модели Product, PriceHistory
    ├── updater.py          ← Ozon ninja парсер (Playwright)
    ├── worker.py           ← фоновый запуск из бота
    ├── agent.py            ← AI агент (Grok / Claude)
    ├── ai_analyzer.py      ← статистика, прогноз, алерты
    ├── exporter.py         ← CSV и Excel экспорт
    ├── reporter.py         ← HTML отчёт
    ├── searcher.py         ← поиск товаров на Озоне
    └── parsers/
        ├── base.py         ← BaseParser + ProductData
        ├── ozon.py         ← Ozon через Playwright
        ├── wildberries.py  ← WB через API
        └── router.py       ← автовыбор парсера по URL
```
